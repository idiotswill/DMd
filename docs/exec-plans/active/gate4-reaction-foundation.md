# Gate4 — Owned work and reaction prerequisites

Status: implementation authored; final integration/review/verification pending.
Writer: root. Branch: codex/gate4-reaction-ready-runtime. PR38 targets main.
Authority: AGENTS, product definition, Gate04, ADR026/028 and the unchanged umbrella
plan gate4-reaction-ready-runtime.md. This plan bounds one reviewable PR, not the gate.

## Objective and scope

Preserve accepted legacy tactical commands and their exact unresolved state while
adding explicit current execution, source-bound defenses and controller-owned Ready
lifetime. Confine ordering permissions to their real causal descendants. These are
production persistence/authority changes required before nested reaction work.

Included: strict live Begin/idle host upgrade; genuine historical declared Second
Wind fixture and original-envelope retry; retained work ancestry; source Mage,
Counterspell/Shield descriptors, component waivers, shared Protective Magic grants
and typed source defenses; pure explicit ordering; paid physical Ready declaration,
own-Start expiry and controller-only abandonment with private desktop projection.
No UI or completion statement may represent those prerequisites as playable Shield,
Counterspell, Ready release or held magic. The follow-up plan owns all such execution.

## Acceptance

- Original saved Begin and direct/declared actions replay with byte-preserved missing
  legacy fields. Existing dice/choices finish before explicit upgrade. Fresh clients
  cannot select weak legacy semantics; original accepted retries still return first.
- Work ancestry cannot transfer an area's ordering authority to independent work.
- Source/profile/equipment and spell defense helpers derive their actual grants,
  costs and lifetimes. Unsupported ordinary reaction casting remains refused.
- Physical Ready pays its Action once, expires correctly and can be abandoned only
  by its controller while idle. No refund, time advance or unrelated effect cleanup.
- Real file-SQLite reopening, private full-DTO/revision preservation, accepted retry,
  independent continuation and hostile restore rejection; actual desktop controls.
- Integrate verified Medicine main, initialize its work ancestry and review the full
  composition. Run canonical local verification, desktop check/test/build and all six
  exact-head checks. Protected squash, fetched full-tree parity and post-merge checks.

## Evidence and risks

a61e95d foundation passes canonical664 GNU Rust tests, both guards and strict lint;
57 UI tests/static/build; all six source CI checks (667 native Rust plus installer).
Those checks precede Savage/abandonment/Medicine composition and do not verify it.
c39365e passed its40 real table-loop tests and61 native UI tests but failed one new
reducer fixture: zero HP without Prone. 0771b16 fixes that invalid fixture and all six
local Ready cases pass. Complete corrected-source CI is running. Medicine PR39's
production code passes canonical654 GNU/655 Linux/657 native Rust and60 UI tests;
its strengthened barrier regression and final-head checks remain pending.

Only one heavy local process runs. Supporting agents are quota-blocked; root performs
a separate full-diff review. Historical replay is the primary risk. Do not fill old
payload defaults with new semantics, claim helper tests as live reactions or merge
against prior-head checks. This foundation remains in active plans until merged and
its post-merge evidence is recorded; the umbrella and follow-up stay active afterward.

## Exact next action

Finish PR39's strengthened tests and protected merge, import actual main normally,
initialize Medicine work_trace and update its hostile fixture consistently. Complete
combined verification and review. Merge PR38 only against its exact verified head,
then create a fresh live-response branch from fetched main and execute the follow-up.
